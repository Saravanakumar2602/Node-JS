// const copy = require("./data");

// console.log(copy);
// console.log(copy.AllNames);


const {AllNames} = require("./data");
console.log(AllNames);


// when we need access from the other files , we use require and at the same time we need to export the data in the required file